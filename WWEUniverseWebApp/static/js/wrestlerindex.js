// load after our DOM loads
document.addEventListener('DOMContentLoaded', function () {

    // const updateButton = document.getElementById('update-button');
    // if (updateButton) {
    //     updateButton.addEventListener('show.bs.modal', event => {
    //         const button = event.relatedTarget;
    //         const wrestlerId = button.getAttribute('data-bs-wrestler-id');
    //
    //         // apply the wrestler id to the hidden input field
    //         const modal = updateButton.querySelector('.modal-body input[name="wrestler_id"]');
    //         modal.value = wrestlerId;
    //     });
    // }
});